{% set name = "prismatools" %}
{% set version = "0.1.4" %}
{% set python_min = "3.10" %}

package:
  name: {{ name|lower }}
  version: {{ version }}

source:
  url: https://pypi.org/packages/source/{{ name[0] }}/{{ name }}/prismatools-{{ version }}.tar.gz
  sha256: f282a7e6f950d3de4006972ad40f371d7bf435ed84b7c721296bd81b7b6029b1

build:
  noarch: python
  entry_points:
    - prismatools=prismatools.cli:main
  script: {{ PYTHON }} -m pip install . -vv --no-deps --no-build-isolation
  number: 0

requirements:
  host:
    - python {{ python_min }}
    - setuptools >=64
    - setuptools-scm >=8
    - pip
  run:
    - python >={{ python_min }}
    - affine
    - bqplot
    - h5py
    - ipyevents
    - ipyfilechooser
    - ipyleaflet
    - ipywidgets
    - leafmap
    - localtileserver
    - matplotlib
    - numpy
    - pandas
    - rioxarray
    - scikit-learn
    - xarray

test:
  imports:
    - prismatools
  commands:
    - pip check
  requires:
    - pip
    - python {{ python_min }}

about:
  home: https://github.com/gthlor/prismatools
  summary: prismatools is an open-source Python package for reading, analyzing, and visualizing hyperspectral imagery from the PRISMA mission.
  license: MIT
  license_file: LICENSE

extra:
  recipe-maintainers:
    - gthlor
